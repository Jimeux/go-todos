<source>
  @type forward
  port 24224
</source>

<match track.todo.completed>
  type file
  path /fluentd/log/track/todo/completed.log
</match>

<match app.error>
  type file
  path /fluentd/log/app/error.log
</match>
